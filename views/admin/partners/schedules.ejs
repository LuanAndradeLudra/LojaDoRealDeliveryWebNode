<%- include("../partials/header"); %>
  <%- include("../partials/navbar"); %>

    <div class="content-wrapper">
      <!-- Content Header (Page header) -->
      <div class="content-header">
        <div class="container-fluid">
          <% if (msg !=undefined) { %>
            <div class="alert alert-info alert-dismissible">
              <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
              <h5><i class="icon fas fa-info"></i> Alerta!</h5>
              <%= msg %>
            </div>
            <% } %>
              <div class="row mb-2">
                <div class="col-sm-12">
                  <h1 class="m-0">Agendamentos</h1>
                </div><!-- /.col -->
              </div><!-- /.row -->
        </div><!-- /.container-fluid -->
      </div>
      <!-- /.content-header -->

      <!-- Main content -->
      <div class="content">
        <div class="container-fluid">

          <div class="card">
            <!-- /.card-header -->
            <div class="card-body">
              <table id="example1" class="table table-bordered table-striped">
                <thead>
                <tr>
                  <th class="col-2">Id</th>
                  <th class="col-7">Nome</th>
                  <th>Email</th>
                  <th>Telefone</th>
                  <th>Documento</th>
                  <th class="col-3">Ações</th>
                </tr>
                </thead>
                <tbody>
              <% schedules.forEach((schedule) => { %>    
                <tr>
                  <td><%= schedule.id %></td>
                  <td><%= schedule.name %> </td>
                  <td><%= schedule.email %> </td>
                  <td><%= schedule.phone %> </td>
                  <td><%= schedule.document %> </td>
                  <td>
                    <form method="POST" action="/admin/partners/schedules/delete" onsubmit="return confirm('Deseja realmente deletar o agendamento <%= schedule.name %>')">
                      <input type="text" value="<%= schedule.id %>" name="id" readonly required hidden>
                      <input type="text" value="<%= schedule.name %>" name="name" readonly required hidden>
                      <button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></input>
                    </form>
                  </td>
                </tr>
              <% }); %>
                </tbody>
                <tfoot>
                <tr>
                  <th>Id</th>
                  <th>Nome</th>
                  <th>Preço</th>
                  <th>Quantidade</th>
                  <th>Ações</th>
                </tr>
                </tfoot>
              </table>
            </div>
            <!-- /.card-body -->
          </div>
          <!-- /.card -->

        </div>
      </div>
      <!-- /.container-fluid -->
    </div>
    <!-- /.content -->

    <%- include("../partials/footer"); %>